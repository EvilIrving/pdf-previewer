# PDF Viewer

单文件 PDF 阅读器，不用服务器，打开浏览器就能用。

选文件夹或者直接拖几个 PDF 进去。普通文档会自动缩放适应页面，遇到长图或单页 PDF 则改为适应宽度。

缩放手滑：50% 到 200%，随便放大缩小。翻到哪页就记到哪页，下次接着看。大文件也不用等，懒加载只渲染当前可见区域。

右上角有个按钮切换深色模式。

---

**技术：** Vue 3 + Tailwind CSS + PDF.js

---

**快捷键：**
- `+` / `-` — 缩放
- `Alt + ←` / `Alt + →` — 切换文件
